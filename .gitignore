node_modules/
dist/
out/
# Downloaded STT weights (~467MB). Fetched by scripts/, never versioned.
models/
# Local transcripts and demo screenshots — user data, not source.
demo-shots/
# HUD screenshots from `npm run shots` - regenerated output, not source.
# The scenes that produce them ARE source (scripts/hud-shot.js).
docs/shots/
*.log
.DS_Store
.env
.dev.vars
%TEMP%/
