# Desktop verify checklist (human)

Do **not** claim these green from unit tests alone.

1. `npm start` — liquid-glass HUD readable over a browser window.
2. Arm mic (Ctrl+\`) — LIVE line updates; no continuous cloud stream.
3. Ask AI — insight shows `Planned via … · gemini-…` when OV has Gemini key (or honest fallback).
4. "Fill this form with my profile" on a local HTML form:
   - enquire if fields missing
   - privacy veil during type burst
   - Esc aborts
5. Live UIA: click a named Windows control once.
6. General mode: cannot act; switch to Agent to run.

OpenVault: put `GEMINI_API_KEY` yourself (no unattended copy).
Cortex: `PACK=dms` for `/dms/secure` + computer-use.
