# Ship + UACC (Pointer copy)

Canonical: `D:\Netie\Internal\Agents\SHIP_SPACESHIP.md`

Pointer is HUD + Cortex `/dms/secure` confirm. UACC is the only OS mouse. Playwright is the only browser DOM driver.

## Order

1. `get_screen_info` (UACC) or `browser_tabs` (Playwright).
2. Chrome DOM (Spaceship, cPanel, Flow, netie.ai) -> Playwright.
3. Cursor / Explorer / OS dialog -> UACC. Type via clipboard + Ctrl+V.
4. Auth wall on a guest/isolated Chrome -> wrong browser. Use the signed-in window (`oojianhongg@gmail.com`) via UACC, or Playwright persistent profile. Do not type the password into cursor-ide-browser.
5. Never store JWT / FTP password in Pointer memory files.

## Spaceship path

Launchpad -> Web Hosting -> netie.ai -> cPanel access.
Token URL: `https://www.spaceship.com/hosting/tools/cpanel-access/`
FTP jail must be `/home/ffvtfuqcxb/netie.ai`, not `public_html`.

## Flow dives (laptop cinematic)

Use Google Flow on the operator Google account. Laptop dives belong in `flow-frames/hero-dive/`, not `/suite/`.
Suite tape stays `ops-tape.mp4`. New dives spend Flow credits -- operator confirms first.

Prompts: `D:\Netie\Internal\Agents\FLOW_DIVES.md`
