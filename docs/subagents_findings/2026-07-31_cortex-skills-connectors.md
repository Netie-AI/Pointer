---
keywords: [cortex, skills, MCP, F8, connectors]
main_idea: First add Pointer-facing SkillCards + expand F8 allowlist; discovery catalogs are refs only — no in-process arbitrary MCP servers (P16).
---

# R5 — Cortex skills / plugins / connectors

- SkillCards live in `D:\Cortex\skills\`; discovery refs are catalogs.
- MCP HTTP shim is first-party tools; third-party MCP is P16-gated.
- **Next:** `form-fill-vault`, `entity-clarify`, `adversarial-review` SkillCards; expand `action_types.yaml` beyond `export_pptx` for screen demos.
